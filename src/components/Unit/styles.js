import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    margin: 10,
    padding: 10,
    borderWidth: 1,
    borderColor: '#bbb',
    borderRadius: 5,
  },
  unitTitleText: {
    color: '#bbb',
    fontSize: 28,
    fontWeight: 'bold',
    padding: 10,
  },
  challengesButtonsWrapper: {},
});

export default styles;
